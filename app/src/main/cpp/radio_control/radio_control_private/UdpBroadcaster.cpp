#include <algorithm>
#include "Network.h"
#include "UdpBroadcaster.h"
#include "Trace.h"

extern void die(const char* msg);

/////////////////////////////////////////////////////////////////////////////

UdpBroadcaster::UdpBroadcaster(uint16_t port)
{
    (void)port;
}

/////////////////////////////////////////////////////////////////////////////

void UdpBroadcaster::setInterfaces(const std::vector<HostAddress> &interfaces)
{
    m_interfaces = interfaces;
}

/////////////////////////////////////////////////////////////////////////////

void UdpBroadcaster::broadcast(const ByteArray &packet)
{
    (void)packet;
}

/////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////

void UdpBroadcaster::broadcast(const HostAddress& address, const ByteArray &packet)
{
    (void)address;
    (void)packet;
}

/////////////////////////////////////////////////////////////////////////////
